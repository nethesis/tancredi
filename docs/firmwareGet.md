# GET /firmware

## Simple query

Get the list of firmware files uploaded by the user

    GET /firmware

(empty request body)

Success response:

    Status: 200 OK

```json
[
    "firmware1",
    "firmware2",
    "firmware3"
]
```

